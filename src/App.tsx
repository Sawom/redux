import { useState } from "react";

function App() {
  const [count, setCount] = useState(0);

  return (
    <>
      <h1> react with redux </h1>

      <div>
        
      </div>

    </>
  );
}

export default App;
